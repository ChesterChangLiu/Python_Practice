num_to_square = int(input())
def compute_square(num_to_square):
    return num_to_square * num_to_square


num_squared = compute_square(num_to_square)

print(num_to_square, 'squared is', num_squared)
